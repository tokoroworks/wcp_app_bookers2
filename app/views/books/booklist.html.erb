<div class="container">
  <div class="row">
    <div class="col">
      <%= render 'users/userinfo', l_user: @user %>
      <%= render 'books/new', book: @book %>
    </div>
    <div class="col">
      <h1>Books</h1>
  <table class="table">
    <tbody>
      <tr>
        <th>title</th>
        <th>opinion</th>
        <th>favorite</th>
      </tr>
      <% @books.each do |bk| %>
      <tr>
        byebug
        <td><%= link_to bk.title, books_path(bk.id) %></td>
        <td><%= bk.body %></td>
        <% if bk.favorited_by?(current_user,bk) %>
          <td>
            <%= link_to books_favorites_path(book), method: :delete do %>
              💛<%= bk.favorites.count %>いいね
            <% end %>
          </td>
        <% else %>
          <td>
            <%= link_to books_favorites_path(book), method: :post do %>
              ♡<%= bk.favorites.count %>いいね
            <% end %>
          </td>
        <% end %>
      </tr>
      <% end %>
    </tbody>
  </table>
    </div>
  </div>
</div>